<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: 'Helvetica Neue', Arial, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { text-align: center; margin-bottom: 30px; }
    .header h1 { color: #2c3e50; }
    .content { margin-bottom: 30px; }
    .order-details { background: #f8f9fa; padding: 20px; border-radius: 5px; margin-bottom: 20px; }
    .footer { text-align: center; margin-top: 30px; color: #777; font-size: 0.9em; }
    .button { display: inline-block; padding: 10px 20px; background: #3498db; color: white; text-decoration: none; border-radius: 5px; margin-top: 20px; }
  </style>
</head>
<body>
  <div class="header">
    <h1>Thank you for your order!</h1>
  </div>

  <div class="content">
    <p>Dear {{customerName}},</p>
    <p>We've received your payment for Order #{{orderNumber}} placed on {{orderDate}}.</p>
    
    <div class="order-details">
      <h3>Order Summary</h3>
      <p><strong>Total Amount:</strong> BDT {{totalAmount}}</p>
      
      {{#if items.length}}
      <h4>Items:</h4>
      <ul>
        {{#each items}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      {{/if}}

      {{#if appointments.length}}
      <h4>Appointments:</h4>
      <ul>
        {{#each appointments}}
        <li>{{date}} at {{time}}</li>
        {{/each}}
      </ul>
      {{/if}}
    </div>

    <p>Your invoice is attached to this email. Please find the PDF attachment for your records.</p>
    <p>If you have any questions about your order, please don't hesitate to contact our support team.</p>
  </div>

  <div class="footer">
    <p>Best regards,</p>
    <p>The Team</p>
  </div>
</body>
</html>